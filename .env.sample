# npm publish 用トークン設定
# - このファイルはサンプルです。.env にコピーしてから編集してください。
# - NPM_TOKEN には npm レジストリ用のアクセストークンを設定します（公開リポジトリでは automation トークン推奨）。
# - DevContainer 内で npm publish を実行する場合も、この値を参照して認証します。
# - 実際のトークンはコミットしないでください（`.env` は Git 管理外です）。
NPM_TOKEN=your-npm-token-here
