// jest-dom は DOM ノードを検証するためのカスタム matcher を追加します。
// たとえば次のようなアサーションが利用できます:
// expect(element).toHaveTextContent(/react/i)
// 詳細: https://github.com/testing-library/jest-dom
import "@testing-library/jest-dom/extend-expect";
